interface:
  display_name: "삼삼엠투 소비자원 답변서"
  short_description: "소보원·1372 답변서를 지정 Google 문서에 직접 작성하거나 새 사본으로 생성"
  default_prompt: "Use $consumer-agency-response to review the complaint, payment details, and contract notes, then write the response in the Google Doc I explicitly identify for editing; if I request a new document instead, copy the fixed master and fill that copy."
  icon_small: "assets/icon.svg"
  icon_large: "assets/icon.svg"
policy:
  allow_implicit_invocation: true
